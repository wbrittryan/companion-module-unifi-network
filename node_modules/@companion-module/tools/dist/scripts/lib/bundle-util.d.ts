import * as esbuild from 'esbuild';
import type { ModuleBuildConfig } from '../../build-config.js';
export interface ModuleBuildDefinition {
    moduleDir: string;
    packageJson: Record<string, unknown>;
    buildConfig: ModuleBuildConfig;
    entryPoints: Record<string, string>;
    externals: string[];
}
export declare function loadModuleBuildDefinition(moduleDir: string): Promise<ModuleBuildDefinition>;
export declare function createEsbuildOptions(definition: ModuleBuildDefinition, overrides: Pick<esbuild.BuildOptions, 'outdir' | 'write' | 'minify' | 'sourcemap'>): esbuild.BuildOptions;
