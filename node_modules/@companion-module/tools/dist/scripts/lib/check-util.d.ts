export declare function checkPackage<M>(options: {
    validateManifest: (manifest: M, looseChecks: boolean) => void;
    cwd?: string;
    ignoreLicenseRules?: boolean;
    stderr?: Pick<NodeJS.WriteStream, 'write' | 'isTTY'>;
}): Promise<void>;
