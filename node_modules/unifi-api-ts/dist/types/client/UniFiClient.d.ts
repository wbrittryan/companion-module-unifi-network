/**
 * Main UniFi API Client
 *
 * A comprehensive TypeScript client for the UniFi Controller API, providing access to all
 * UniFi network management functionality. This client handles authentication, session management,
 * and provides strongly-typed methods for interacting with UniFi controllers.
 *
 * @example
 * ```typescript
 * import { UniFiClient } from 'unifi-api-ts';
 *
 * const client = new UniFiClient({
 *   baseUrl: 'https://unifi.example.com:8443',
 *   username: 'admin',
 *   password: 'password',
 *   site: 'default'
 * });
 *
 * // Login and get devices
 * await client.login();
 * const devices = await client.listDevices();
 * console.log(`Found ${devices.length} devices`);
 * ```
 *
 * @since 1.0.0
 */
import { UniFiClientConfig } from '../types/config';
import { HTTPClient, SessionManager } from '../http';
import { AuthenticationAPI } from '../api/authentication';
import { DeviceManagementAPI } from '../api/device-management';
import { ClientManagementAPI } from '../api/client-management';
import { NetworkManagementAPI } from '../api/network-management';
import { SiteManagementAPI } from '../api/site-management';
import { UserManagementAPI } from '../api/user-management';
import { SecurityAPI } from '../api/security';
import { StatisticsAPI } from '../api/statistics';
export declare class UniFiClient {
    private config;
    private httpClient;
    private sessionManager;
    private authenticationAPI;
    private deviceManagementAPI;
    private clientManagementAPI;
    private networkManagementAPI;
    private siteManagementAPI;
    private userManagementAPI;
    private securityAPI;
    private statisticsAPI;
    /**
     * Creates a new UniFi API client instance
     *
     * @param config - Configuration object for the UniFi client
     * @param config.baseUrl - Base URL of the UniFi controller (e.g., 'https://unifi.example.com:8443')
     * @param config.username - Username for authentication
     * @param config.password - Password for authentication
     * @param config.site - Site name (defaults to 'default')
     * @param config.timeout - Request timeout in milliseconds (defaults to 30000)
     * @param config.verifySsl - Whether to verify SSL certificates (defaults to true)
     * @param config.debug - Enable debug logging (defaults to false)
     *
     * @throws {ConfigurationError} When required configuration parameters are missing or invalid
     *
     * @example
     * ```typescript
     * const client = new UniFiClient({
     *   baseUrl: 'https://192.168.1.1:8443',
     *   username: 'admin',
     *   password: 'mypassword',
     *   site: 'default',
     *   timeout: 10000,
     *   verifySsl: false
     * });
     * ```
     */
    constructor(config: UniFiClientConfig);
    /**
     * Validates the client configuration
     *
     * @private
     * @param config - Configuration to validate
     * @throws {ConfigurationError} When configuration is invalid
     */
    private static validateConfig;
    /**
     * Authenticates with the UniFi controller
     *
     * Establishes a session with the UniFi controller using the provided credentials.
     * This method must be called before making any API requests that require authentication.
     *
     * @returns Promise that resolves to true when login is successful
     * @throws {AuthenticationError} When login credentials are invalid
     * @throws {NetworkError} When unable to connect to the controller
     * @throws {ConfigurationError} When client configuration is invalid
     *
     * @example
     * ```typescript
     * try {
     *   await client.login();
     *   console.log('Successfully logged in');
     * } catch (error) {
     *   if (error instanceof AuthenticationError) {
     *     console.error('Invalid credentials');
     *   } else {
     *     console.error('Login failed:', error.message);
     *   }
     * }
     * ```
     */
    login(): Promise<boolean>;
    /**
     * Logs out from the UniFi controller
     *
     * Terminates the current session with the UniFi controller and clears all
     * authentication cookies. This method should be called when finished with the client.
     *
     * @returns Promise that resolves to true when logout is successful
     *
     * @example
     * ```typescript
     * // Always logout when done
     * try {
     *   await client.logout();
     *   console.log('Successfully logged out');
     * } catch (error) {
     *   console.warn('Logout failed, but session cleared locally');
     * }
     * ```
     */
    logout(): Promise<boolean>;
    /**
     * Checks if the client is currently authenticated
     *
     * Returns the current authentication status without making any network requests.
     * Note that this only reflects the local session state and doesn't verify
     * if the session is still valid on the server.
     *
     * @returns true if the client has an active session, false otherwise
     *
     * @example
     * ```typescript
     * if (!client.isAuthenticated()) {
     *   await client.login();
     * }
     *
     * // Now safe to make API calls
     * const devices = await client.listDevices();
     * ```
     */
    isAuthenticated(): boolean;
    /**
     * Retrieves current session information
     *
     * Returns detailed information about the current session including
     * authentication status, login time, last activity, and user details.
     *
     * @returns Session information object
     * @returns sessionInfo.isAuthenticated - Whether the session is active
     * @returns sessionInfo.loginTime - When the session was established
     * @returns sessionInfo.lastActivity - Last API activity timestamp
     * @returns sessionInfo.username - Authenticated username
     * @returns sessionInfo.site - Current site context
     *
     * @example
     * ```typescript
     * const sessionInfo = client.getSessionInfo();
     * console.log(`Logged in as: ${sessionInfo.username}`);
     * console.log(`Site: ${sessionInfo.site}`);
     * console.log(`Session active: ${sessionInfo.isAuthenticated}`);
     * ```
     */
    getSessionInfo(): import("../http").SessionInfo;
    /**
     * Gets the configured site name
     *
     * Returns the site name that this client instance is configured to work with.
     * All API operations will be performed in the context of this site.
     *
     * @returns The site name (defaults to 'default' if not specified in config)
     *
     * @example
     * ```typescript
     * const site = client.getSite();
     * console.log(`Working with site: ${site}`);
     * ```
     */
    getSite(): string;
    /**
     * Gets the underlying HTTP client instance
     *
     * Provides access to the internal HTTP client for advanced usage scenarios
     * such as custom request configuration or direct API calls.
     *
     * @returns The HTTP client instance
     *
     * @example
     * ```typescript
     * const httpClient = client.getHttpClient();
     *
     * // Make a custom API call
     * const response = await httpClient.get('/api/s/default/stat/health');
     * ```
     */
    getHttpClient(): HTTPClient;
    /**
     * Gets the authentication API instance
     *
     * Provides access to the authentication API for advanced authentication
     * scenarios such as custom authentication flows or session monitoring.
     *
     * @returns The authentication API instance
     *
     * @example
     * ```typescript
     * const authAPI = client.getAuthenticationAPI();
     *
     * // Check if session needs refresh
     * await authAPI.ensureAuthenticated();
     * ```
     */
    getAuthenticationAPI(): AuthenticationAPI;
    /**
     * Gets the session manager instance
     *
     * Provides access to the internal session manager for advanced session
     * management scenarios such as custom authentication flows or session monitoring.
     *
     * @returns The session manager instance
     *
     * @example
     * ```typescript
     * const sessionManager = client.getSessionManager();
     *
     * // Check if session needs refresh
     * await sessionManager.ensureAuthenticated();
     * ```
     */
    getSessionManager(): SessionManager;
    /**
     * Gets the device management API instance
     *
     * Provides access to the device management API for advanced device
     * management scenarios such as custom device operations or bulk operations.
     *
     * @returns The device management API instance
     *
     * @example
     * ```typescript
     * const deviceAPI = client.getDeviceManagementAPI();
     *
     * // Use device management API directly
     * const devices = await deviceAPI.list_devices();
     * ```
     */
    getDeviceManagementAPI(): DeviceManagementAPI;
    /**
     * Gets the client management API instance
     *
     * Provides access to the client management API for advanced client
     * management scenarios such as bulk client operations or guest management.
     *
     * @returns The client management API instance
     *
     * @example
     * ```typescript
     * const clientAPI = client.getClientManagementAPI();
     *
     * // Use client management API directly
     * const clients = await clientAPI.list_users();
     * await clientAPI.block_sta('default', 'aa:bb:cc:dd:ee:ff');
     * ```
     */
    getClientManagementAPI(): ClientManagementAPI;
    /**
     * Get the Network Management API instance
     *
     * Provides access to network configuration operations including network creation,
     * VLAN management, routing configuration, DNS settings, and port forwarding.
     *
     * @returns NetworkManagementAPI instance
     *
     * @example
     * ```typescript
     * const networkAPI = client.getNetworkManagementAPI();
     *
     * // Use network management API directly
     * const networks = await networkAPI.list_networkconf();
     * await networkAPI.create_network({
     *   name: 'Guest Network',
     *   purpose: 'guest',
     *   ip_subnet: '192.168.100.1/24'
     * });
     * ```
     */
    getNetworkManagementAPI(): NetworkManagementAPI;
    /**
     * Get the Site Management API instance
     *
     * Provides access to site management operations including site creation,
     * deletion, configuration, and statistics.
     *
     * @returns Site Management API instance
     *
     * @example
     * ```typescript
     * const siteAPI = client.getSiteManagementAPI();
     *
     * // Use site management API directly
     * const sites = await siteAPI.list_sites();
     * await siteAPI.create_site('New Branch Office');
     * await siteAPI.set_site_name('Updated Office Name');
     * const stats = await siteAPI.stat_sites();
     * ```
     */
    getSiteManagementAPI(): SiteManagementAPI;
    /**
     * Get the User Management API instance
     *
     * Provides access to user, user group, and administrator management operations
     * including user creation, group management, and admin permissions.
     *
     * @returns User Management API instance
     *
     * @example
     * ```typescript
     * const userAPI = client.getUserManagementAPI();
     *
     * // Use user management API directly
     * const users = await userAPI.list_users();
     * await userAPI.create_user('aa:bb:cc:dd:ee:ff', 'group_id', 'John Doe');
     * const groups = await userAPI.list_usergroups();
     * ```
     */
    getUserManagementAPI(): UserManagementAPI;
    /**
     * Get the Security API instance
     *
     * Provides access to security-related operations including firewall management,
     * IPS/IDS settings, RADIUS authentication, and access controls.
     *
     * @returns Security API instance
     *
     * @example
     * ```typescript
     * const securityAPI = client.getSecurityAPI();
     *
     * // Use security API directly
     * const firewallGroups = await securityAPI.list_firewallgroups();
     * await securityAPI.create_firewallgroup('BlockedIPs', 'address-group', ['192.168.1.100']);
     * const radiusAccounts = await securityAPI.list_radius_accounts();
     * ```
     */
    getSecurityAPI(): SecurityAPI;
    /**
     * Get the Statistics API instance
     *
     * Provides access to statistics and monitoring operations including system health,
     * event monitoring, and various time-based statistics.
     *
     * @returns Statistics API instance
     *
     * @example
     * ```typescript
     * const statsAPI = client.getStatisticsAPI();
     *
     * // Use statistics API directly
     * const sysInfo = await statsAPI.stat_sysinfo();
     * const events = await statsAPI.list_events();
     * const health = await statsAPI.list_health();
     * ```
     */
    getStatisticsAPI(): StatisticsAPI;
    listUsers(clientMac?: string, options?: {
        signal?: AbortSignal;
    }): Promise<import("../types").UniFiClient[]>;
    listDevices(macs?: string | string[], options?: {
        signal?: AbortSignal;
    }): Promise<import("..").UniFiDevice[]>;
    statSysinfo(options?: {
        signal?: AbortSignal;
    }): Promise<import("..").SystemInfo[]>;
    adoptDevice(macs: string | string[], options?: {
        signal?: AbortSignal;
    }): Promise<boolean>;
    advancedAdoptDevice(mac: string, ip: string, username: string, password: string, url: string, port?: number, sshKeyVerify?: boolean, options?: {
        signal?: AbortSignal;
    }): Promise<boolean>;
    blockSta(mac: string, options?: {
        signal?: AbortSignal;
    }): Promise<boolean>;
    forceProvision(macs: string | string[], options?: {
        signal?: AbortSignal;
    }): Promise<boolean>;
    forgetSta(macs: string | string[], options?: {
        signal?: AbortSignal;
    }): Promise<boolean>;
    authorizeGuest(mac: string, minutes: number, up?: number, down?: number, megabytes?: number, apMac?: string, options?: {
        signal?: AbortSignal;
    }): Promise<boolean>;
    extendGuestValidity(guestId: string, options?: {
        signal?: AbortSignal;
    }): Promise<boolean>;
    createWlan(name: string, xPassphrase: string, usergroupId: string, wlangroupId: string, enabled?: boolean, hideSsid?: boolean, isGuest?: boolean, security?: string, wpaMode?: string, wpaEnc?: string, vlanEnabled?: boolean, vlanId?: string, uapsdEnabled?: boolean, scheduleEnabled?: boolean, schedule?: any[], apGroupIds?: string[], additionalPayload?: any, options?: {
        signal?: AbortSignal;
    }): Promise<boolean>;
    deleteWlan(wlanId: string, options?: {
        signal?: AbortSignal;
    }): Promise<boolean>;
    createNetwork(payload: any, options?: {
        signal?: AbortSignal;
    }): Promise<any>;
    deleteNetwork(networkId: string, options?: {
        signal?: AbortSignal;
    }): Promise<boolean>;
    listNetworks(networkId?: string, options?: {
        signal?: AbortSignal;
    }): Promise<any>;
    updateNetworkSettings(networkId: string, payload: any, options?: {
        signal?: AbortSignal;
    }): Promise<boolean>;
    listRouting(routeId?: string, options?: {
        signal?: AbortSignal;
    }): Promise<any>;
    listPortConfigurations(options?: {
        signal?: AbortSignal;
    }): Promise<any>;
    listPortForwarding(options?: {
        signal?: AbortSignal;
    }): Promise<any>;
    listPortForwardingStats(options?: {
        signal?: AbortSignal;
    }): Promise<any>;
    createDnsRecord(recordType: 'A' | 'AAAA' | 'MX' | 'TXT' | 'SRV' | 'NS', value: string, key: string, ttl?: number, enabled?: boolean, options?: {
        signal?: AbortSignal;
    }): Promise<any>;
    listDnsRecords(options?: {
        signal?: AbortSignal;
    }): Promise<any>;
    deleteDnsRecord(recordId: string, options?: {
        signal?: AbortSignal;
    }): Promise<boolean>;
    createDynamicDns(payload: any, options?: {
        signal?: AbortSignal;
    }): Promise<boolean>;
    listDynamicDns(options?: {
        signal?: AbortSignal;
    }): Promise<any>;
    updateDynamicDns(dynamicDnsId: string, payload: any, options?: {
        signal?: AbortSignal;
    }): Promise<boolean>;
    createSite(description: string, options?: {
        signal?: AbortSignal;
    }): Promise<any>;
    deleteSite(siteId: string, options?: {
        signal?: AbortSignal;
    }): Promise<boolean>;
    generateBackup(options?: {
        signal?: AbortSignal;
    }): Promise<boolean>;
    generateBackupSite(options?: {
        signal?: AbortSignal;
    }): Promise<boolean>;
    downloadBackup(filepath: string, options?: {
        signal?: AbortSignal;
    }): Promise<any>;
    listBackups(options?: {
        signal?: AbortSignal;
    }): Promise<any>;
    updateOsConsole(options?: {
        signal?: AbortSignal;
    }): Promise<boolean>;
    getUpdateOsConsole(options?: {
        signal?: AbortSignal;
    }): Promise<any>;
    listSettings(options?: {
        signal?: AbortSignal;
    }): Promise<any>;
    listSelf(options?: {
        signal?: AbortSignal;
    }): Promise<any>;
    setSuperIdentitySettingsBase(payload: any, options?: {
        signal?: AbortSignal;
    }): Promise<boolean>;
    setSuperMgmtSettingsBase(payload: any, options?: {
        signal?: AbortSignal;
    }): Promise<boolean>;
    setSuperSmtpSettingsBase(payload: any, options?: {
        signal?: AbortSignal;
    }): Promise<boolean>;
    download_backup(filepath: string, options?: {
        signal?: AbortSignal;
    }): Promise<any>;
    generate_backup(options?: {
        signal?: AbortSignal;
    }): Promise<boolean>;
    generate_backup_site(options?: {
        signal?: AbortSignal;
    }): Promise<boolean>;
    list_backups(options?: {
        signal?: AbortSignal;
    }): Promise<any>;
    update_os_console(options?: {
        signal?: AbortSignal;
    }): Promise<boolean>;
    get_update_os_console(options?: {
        signal?: AbortSignal;
    }): Promise<any>;
    list_settings(options?: {
        signal?: AbortSignal;
    }): Promise<any>;
    list_self(options?: {
        signal?: AbortSignal;
    }): Promise<any>;
    set_super_identity_settings_base(payload: any, options?: {
        signal?: AbortSignal;
    }): Promise<boolean>;
    set_super_mgmt_settings_base(payload: any, options?: {
        signal?: AbortSignal;
    }): Promise<boolean>;
    set_super_smtp_settings_base(payload: any, options?: {
        signal?: AbortSignal;
    }): Promise<boolean>;
    archiveAlarm(alarmId?: string, options?: {
        signal?: AbortSignal;
    }): Promise<boolean>;
    countAlarms(archived?: boolean, options?: {
        signal?: AbortSignal;
    }): Promise<any>;
    createUser(mac: string, userGroupId: string, name?: string, note?: string, isGuest?: boolean, isWired?: boolean, options?: {
        signal?: AbortSignal;
    }): Promise<any>;
    createUsergroup(groupName: string, groupDn?: number, groupUp?: number, options?: {
        signal?: AbortSignal;
    }): Promise<any>;
    deleteUsergroup(groupId: string, options?: {
        signal?: AbortSignal;
    }): Promise<boolean>;
    createVoucher(minutes: number, count?: number, quota?: number, note?: string, up?: number, down?: number, megabytes?: number, options?: {
        signal?: AbortSignal;
    }): Promise<any>;
    createTag(name: string, macs?: string[], options?: {
        signal?: AbortSignal;
    }): Promise<boolean>;
    deleteTag(tagId: string, options?: {
        signal?: AbortSignal;
    }): Promise<boolean>;
    createApgroup(groupName: string, deviceMacs?: string[], options?: {
        signal?: AbortSignal;
    }): Promise<boolean>;
    deleteApgroup(groupId: string, options?: {
        signal?: AbortSignal;
    }): Promise<boolean>;
    editApgroup(groupId: string, groupName: string, deviceMacs: string[], options?: {
        signal?: AbortSignal;
    }): Promise<any>;
    create_apgroup(groupName: string, deviceMacs?: string[], options?: {
        signal?: AbortSignal;
    }): Promise<boolean>;
    delete_apgroup(groupId: string, options?: {
        signal?: AbortSignal;
    }): Promise<boolean>;
    edit_apgroup(groupId: string, groupName: string, deviceMacs: string[], options?: {
        signal?: AbortSignal;
    }): Promise<any>;
    createFirewallgroup(groupName: string, groupType: 'address-group' | 'ipv6-address-group' | 'port-group', groupMembers?: string[], options?: {
        signal?: AbortSignal;
    }): Promise<any>;
    deleteFirewallgroup(groupId: string, options?: {
        signal?: AbortSignal;
    }): Promise<boolean>;
    editFirewallgroup(groupId: string, siteId: string, groupName: string, groupType: 'address-group' | 'ipv6-address-group' | 'port-group', groupMembers?: string[], options?: {
        signal?: AbortSignal;
    }): Promise<boolean>;
    createRadiusAccount(name: string, xPassword: string, tunnelType?: number, tunnelMediumType?: number, vlan?: number, options?: {
        signal?: AbortSignal;
    }): Promise<any>;
    deleteRadiusAccount(accountId: string, options?: {
        signal?: AbortSignal;
    }): Promise<boolean>;
    createDynamicdns(payload: any, options?: {
        signal?: AbortSignal;
    }): Promise<boolean>;
    createHotspotop(name: string, xPassword: string, note?: string, options?: {
        signal?: AbortSignal;
    }): Promise<boolean>;
    cancelMigrateDevice(macs: string | string[], options?: {
        signal?: AbortSignal;
    }): Promise<boolean>;
    cancelRollingUpgrade(options?: {
        signal?: AbortSignal;
    }): Promise<boolean>;
    checkControllerUpdate(options?: {
        signal?: AbortSignal;
    }): Promise<any>;
    checkFirmwareUpdate(options?: {
        signal?: AbortSignal;
    }): Promise<boolean>;
    deleteDevice(mac: string, options?: {
        signal?: AbortSignal;
    }): Promise<boolean>;
    disableAp(apId: string, disable: boolean, options?: {
        signal?: AbortSignal;
    }): Promise<boolean>;
    disableWlan(wlanId: string, disable: boolean, options?: {
        signal?: AbortSignal;
    }): Promise<boolean>;
    editClientFixedip(clientId: string, useFixedip: boolean, networkId?: string, fixedIp?: string, options?: {
        signal?: AbortSignal;
    }): Promise<any>;
    editClientName(clientId: string, name: string, options?: {
        signal?: AbortSignal;
    }): Promise<any>;
    editUsergroup(groupId: string, siteId: string, groupName: string, groupDn?: number, groupUp?: number, options?: {
        signal?: AbortSignal;
    }): Promise<any>;
    assignExistingAdmin(adminId: string, readonly?: boolean, deviceAdopt?: boolean, deviceRestart?: boolean, options?: {
        signal?: AbortSignal;
    }): Promise<boolean>;
    deleteAdmin(adminId: string, options?: {
        signal?: AbortSignal;
    }): Promise<boolean>;
    cmdStat(command: string, options?: {
        signal?: AbortSignal;
    }): Promise<boolean>;
    customApiRequest(path: string, method?: 'GET' | 'POST' | 'PUT' | 'DELETE' | 'PATCH', payload?: any, returnType?: 'array' | 'boolean', options?: {
        signal?: AbortSignal;
    }): Promise<any>;
    getClassVersion(): string;
    getConnectionTimeout(): number;
    getCookie(): string;
    getCookies(): string;
    getCookiesCreatedAt(): number;
    getCurlHttpVersion(): string;
    getCurlMethod(): string;
    getCurlRequestTimeout(): number;
    getCurlSslVerifyHost(): number;
    getCurlSslVerifyPeer(): boolean;
    listAllAdmins(options?: {
        signal?: AbortSignal;
    }): Promise<any>;
    listApgroups(options?: {
        signal?: AbortSignal;
    }): Promise<any>;
    listAps(mac?: string, options?: {
        signal?: AbortSignal;
    }): Promise<any>;
    listClients(mac?: string, options?: {
        signal?: AbortSignal;
    }): Promise<any>;
    listClientsHistory(historyhours?: number, start?: number, end?: number, mac?: string, options?: {
        signal?: AbortSignal;
    }): Promise<any>;
    listCountryCodes(options?: {
        signal?: AbortSignal;
    }): Promise<any>;
    listCurrentChannels(options?: {
        signal?: AbortSignal;
    }): Promise<any>;
    listDashboard(fiveMinutes?: boolean, options?: {
        signal?: AbortSignal;
    }): Promise<any>;
    listDeviceNameMappings(options?: {
        signal?: AbortSignal;
    }): Promise<any>;
    listDeviceStates(options?: {
        signal?: AbortSignal;
    }): Promise<any>;
    listDevicesBasic(mac?: string, options?: {
        signal?: AbortSignal;
    }): Promise<any>;
    listDpiStats(options?: {
        signal?: AbortSignal;
    }): Promise<any>;
    listDpiStatsFiltered(type?: 'by_app' | 'by_cat', catFilter?: string, options?: {
        signal?: AbortSignal;
    }): Promise<any>;
    listDynamicdns(options?: {
        signal?: AbortSignal;
    }): Promise<any>;
    listExtension(options?: {
        signal?: AbortSignal;
    }): Promise<any>;
    listFingerprintDevices(options?: {
        signal?: AbortSignal;
    }): Promise<any>;
    listFirewallgroups(options?: {
        signal?: AbortSignal;
    }): Promise<any>;
    listFirewallrules(options?: {
        signal?: AbortSignal;
    }): Promise<any>;
    listFirmware(type?: 'available' | 'cached', options?: {
        signal?: AbortSignal;
    }): Promise<any>;
    listGuests(within?: number, options?: {
        signal?: AbortSignal;
    }): Promise<any>;
    listHealth(options?: {
        signal?: AbortSignal;
    }): Promise<any>;
    listHotspotop(options?: {
        signal?: AbortSignal;
    }): Promise<any>;
    listKnownRogueaps(options?: {
        signal?: AbortSignal;
    }): Promise<any>;
    listModels(options?: {
        signal?: AbortSignal;
    }): Promise<any>;
    listPortconf(options?: {
        signal?: AbortSignal;
    }): Promise<any>;
    listPortforwardStats(options?: {
        signal?: AbortSignal;
    }): Promise<any>;
    listWlanconf(wlan_id?: string, options?: {
        signal?: AbortSignal;
    }): Promise<any[]>;
    listNetworkconf(network_id?: string, options?: {
        signal?: AbortSignal;
    }): Promise<any[]>;
    listSites(options?: {
        signal?: AbortSignal;
    }): Promise<any[]>;
    listEvents(historyhours?: number, start?: number, end?: number, limit?: number, options?: {
        signal?: AbortSignal;
    }): Promise<any[]>;
    listAlarms(payload?: any, options?: {
        signal?: AbortSignal;
    }): Promise<any[]>;
    migrateDevice(macs: string | string[], inform_url: string, options?: {
        signal?: AbortSignal;
    }): Promise<boolean>;
    moveDevice(mac: string, site_id: string, options?: {
        signal?: AbortSignal;
    }): Promise<boolean>;
    powerCycleSwitchPort(mac: string, port_idx: number, options?: {
        signal?: AbortSignal;
    }): Promise<boolean>;
    rebootCloudkey(options?: {
        signal?: AbortSignal;
    }): Promise<boolean>;
    reconnectSta(mac: string, options?: {
        signal?: AbortSignal;
    }): Promise<boolean>;
    renameAp(ap_id: string, ap_name: string, options?: {
        signal?: AbortSignal;
    }): Promise<boolean>;
    restartAp(): Promise<boolean>;
    restartDevice(macs: string | string[], reboot_type?: 'soft' | 'hard', options?: {
        signal?: AbortSignal;
    }): Promise<boolean>;
    revokeAdmin(admin_id: string, options?: {
        signal?: AbortSignal;
    }): Promise<boolean>;
    revokeVoucher(voucher_id: string, options?: {
        signal?: AbortSignal;
    }): Promise<boolean>;
    setApRadiosettings(ap_id: string, radio: string, channel: number, ht: number, tx_power_mode: string, tx_power: string, options?: {
        signal?: AbortSignal;
    }): Promise<boolean>;
    setApWlangroup(type_id: 'ng' | 'na', device_id: string, group_id: string, options?: {
        signal?: AbortSignal;
    }): Promise<boolean>;
    setConnectionTimeout(timeout: number): boolean;
    setCookies(_cookies_value: string): void;
    setCurlHttpVersion(_http_version: number): boolean;
    unauthorizeGuest(mac: string, options?: {
        signal?: AbortSignal;
    }): Promise<boolean>;
    delete_admin(admin_id: string, options?: {
        signal?: AbortSignal;
    }): Promise<boolean>;
    edit_usergroup(group_id: string, site_id: string, group_name: string, group_dn?: number, group_up?: number, options?: {
        signal?: AbortSignal;
    }): Promise<any>;
    list_devices(macs?: string | string[], options?: {
        signal?: AbortSignal;
    }): Promise<import("..").UniFiDevice[]>;
    adopt_device(macs: string | string[], options?: {
        signal?: AbortSignal;
    }): Promise<boolean>;
    restart_device(macs: string | string[], reboot_type?: 'soft' | 'hard', options?: {
        signal?: AbortSignal;
    }): Promise<boolean>;
    list_users(client_mac?: string, options?: {
        signal?: AbortSignal;
    }): Promise<import("../types").UniFiClient[]>;
    block_sta(mac: string, options?: {
        signal?: AbortSignal;
    }): Promise<boolean>;
    authorize_guest(mac: string, minutes: number, up?: number, down?: number, megabytes?: number, apMac?: string, options?: {
        signal?: AbortSignal;
    }): Promise<boolean>;
    list_wlanconf(wlan_id?: string, options?: {
        signal?: AbortSignal;
    }): Promise<any>;
    create_wlan(name: string, x_passphrase: string, usergroup_id: string, wlangroup_id: string, enabled?: boolean, hide_ssid?: boolean, is_guest?: boolean, security?: string, wpa_mode?: string, wpa_enc?: string, vlan_enabled?: boolean, vlan_id?: string, uapsd_enabled?: boolean, schedule_enabled?: boolean, schedule?: any[], ap_group_ids?: string[], additional_payload?: any, options?: {
        signal?: AbortSignal;
    }): Promise<boolean>;
    list_sites(options?: {
        signal?: AbortSignal;
    }): Promise<any[]>;
    create_site(description: string, options?: {
        signal?: AbortSignal;
    }): Promise<boolean>;
    list_networkconf(network_id?: string, options?: {
        signal?: AbortSignal;
    }): Promise<any[]>;
    create_network(payload: any, options?: {
        signal?: AbortSignal;
    }): Promise<boolean>;
    stat_sysinfo(options?: {
        signal?: AbortSignal;
    }): Promise<any>;
    list_events(historyhours?: number, start?: number, end?: number, limit?: number, options?: {
        signal?: AbortSignal;
    }): Promise<any[]>;
    list_alarms(payload?: any, options?: {
        signal?: AbortSignal;
    }): Promise<any[]>;
    reconnect_sta(mac: string, options?: {
        signal?: AbortSignal;
    }): Promise<boolean>;
    unauthorize_guest(mac: string, options?: {
        signal?: AbortSignal;
    }): Promise<boolean>;
}
//# sourceMappingURL=UniFiClient.d.ts.map